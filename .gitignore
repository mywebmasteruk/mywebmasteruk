node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
!.env.example

# Raw collector output is derived and regenerated every run.
ops/data/gsc-*.json
ops/data/ga4-*.json
ops/data/crawl.json
ops/data/findings.json

# Kept in git deliberately:
#   ops/data/baseline.json  — the published starting line
#   ops/data/freeze.json    — measurement windows must survive between CI runs
#   ops/reports/*.md        — the human-readable record

.netlify/
